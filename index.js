const divide = (num1 , num2) => 2000 / 100;

    //const (num1 , num2) = () => 2000 / 100;

 const square = (num) => num * num;

    //squire = () => 4* 4;

    const add = (x, y) => x + y;

   // add = () => 50 + 150;
        